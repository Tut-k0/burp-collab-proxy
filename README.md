# Private Burp Collaborator Automation
TODO.

### Running
Configure `ansible/inventory/local` and run `ansible-playbook`:
```bash
cd ansible
ansible-playbook -i inventory/local playbooks/deploy-local.yml
```